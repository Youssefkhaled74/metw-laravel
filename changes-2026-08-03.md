# سجل تغييرات 2026-08-03 (المهام المجدولة الخمسة + لوحة المراقبة)

> هذا الملف يشرح ببساطة ما تم تنفيذه اليوم: تحويل «مهلات الشحن» الخمسة إلى **مهام مجدولة** (Cron Jobs) كاملة، مع **سجل تنفيذ** في قاعدة البيانات، و**صفحة إعدادات** للأدمن، و**صفحات مراقبة** للمهام.

---

## 1) الفكرة العامة

كانت «مهلات الشحن» قبل اليوم خمس قيم افتراضية مذكورة في الملاحظات لكنها لم تعمل تلقائيًا كاملة. اليوم تم:

1. تحويل كل مهلة إلى **مهمة مجدولة** فعلية بكلاس خاص بها.
2. إنشاء **سجل تنفيذ** جديد (`cron_job_logs`) يُسجّل كل تشغيل (نجاح/فشل، المدة، عدد السجلات المعالجة والمتأثرة، وأي خطأ).
3. تخزين قيم المهلات الخمس في **جدول الإعدادات** (`settings`) قابلة للتعديل من الأدمن.
4. إنشاء **صفحة إعدادات** «مؤقتات الشحن» لتعديلها.
5. إنشاء **صفحات مراقبة** تعرض حالة كل مهمة وسجل تنفيذها وزر «تشغيل الآن».

---

## 2) المهام الخمسة

| المفتاح | الوظيفة | التوقيت | المهلة |
|---------|---------|---------|--------|
| `courier-auto-reject` | رفض عروض المندوبين التي لم تُجب خلال **X** ساعة عمل. | كل دقيقة | X = 7 ساعات عمل |
| `courier-close-response-window` | إغلاق نافذة الاستجابة بعد أول قبول خلال **Y** ساعة عمل. | كل دقيقة | Y = 3 ساعات عمل |
| `advance-payments-cancel-unpaid` | إلغاء طلبات **المستخدمين** التي بقيت بانتظار الدفعة المقدمة دون سداد خلال **Z** ساعة فعلية (لا تُلمس طلبات البائعين). | كل 15 دقيقة | Z = 12 ساعة فعلية |
| `sub-shipments-aggregate` | تجميع طلبات الشحن المفتوحة حسب **محافظة المستلم** (مطابقة مع المستودعات النشطة) كل **N** يوم. | يوميًا الساعة 02:00 | N = 3 أيام |
| `shipment-requests-mark-execution-start` | بدء تنفيذ الطلبات **بين المحافظات** التي تأكدت دفعتها المقدمة ولم يبدأ تنفيذها خلال **W** ساعة فعلية (لا يبدأ أبدًا الطلبات غير المدفوعة). | كل 15 دقيقة | W = 24 ساعة فعلية |

### ملاحظات على سلوك المهام

- **المهمتان 1 و2** تستخدمان نفس منطق `CourierTimeoutProcessor` القديم (إنهاء العروض المتجاوزة عبر `CourierAssignmentService::expire`) لكن داخل كلاسات مستقلة.
- **المهمة 3** عند الإلغاء: تُرفض أي دفعة مقدمة معلّقة، وتُلغى العروض المعلّقة، ويُغلق الطلب بحالة `CANCELLED` مع وقت الإغلاق.
- **المهمة 4** لا تحذف أي شيء؛ فقط **تضع علامات** (metadata) على الطلبات المرشحة للتجميع (رقم الدفعة، المستودعات، وقت التجميع). النطاق قابل للتكوين: كل المستودعات / مستودع واحد / مجموعة محافظة واحدة.
- **المهمة 5** تشترط وجود دفعة **مؤكدة** (`CONFIRMED`) وطلبًا بين محافظتين مختلفتين وعدم بدء التنفيذ بعد.

---

## 3) جدول `cron_job_logs` (سجل التنفيذ)

هجرة جديدة `2026_08_03_000001_create_cron_job_logs_table.php`:

| العمود | الوصف |
|--------|-------|
| `job_name` | مفتاح المهمة (مثل `courier-auto-reject`). |
| `status` | `running` / `success` / `failed`. |
| `started_at` / `finished_at` | بداية ونهاية التشغيل. |
| `duration` | المدة بالثواني. |
| `records_processed` | عدد السجلات التي فحصتها المهمة. |
| `records_affected` | عدد السجلات التي غيّرتها. |
| `error_message` | نص الخطأ عند الفشل. |
| `metadata` | JSON بتفاصيل إضافية (مثل دفعات التجميع). |

هجرة ثانية `2026_08_03_000002_add_updated_by_to_settings_table.php` تضيف عمود `updated_by` لجدول الإعدادات لتسجيل **من** عدّل إعدادات المؤقتات (أدمن أو موظف).

---

## 4) بنية الكود

- `app/Contracts/CronJob.php` — واجهة كل مهمة (الاسم + دالة `handle` التي ترجع `CronJobResult`).
- `app/Services/Cron/CronJobResult.php` — ناتج التشغيل (معالجة / متأثرة / بيانات إضافية).
- `app/Services/Cron/CronJobRunner.php` — يُنشئ سطر سجل لكل تشغيل ويحدّثه عند النجاح أو الفشل.
- `app/Services/Cron/CronJobRegistry.php` — تعريف كل المهام (الأسماء، الوصف عربي/إنجليزي، الأيقونة، التوقيت، حساب موعد التشغيل القادم).
- `app/Jobs/Scheduled/*.php` — الكلاسات الخمسة نفسها.
- `app/Console/Commands/RunScheduledJob.php` — أمر `cron:run {job}` لتنفيذ أي مهمة يدويًا أو عبر المجدول.
- `app/Console/Kernel.php` — جدولة المهام الخمس:
  - 1 و2: كل دقيقة.
  - 3 و5: كل 15 دقيقة.
  - 4: يوميًا الساعة 02:00.
  - الكل مع `->name()->withoutOverlapping()->runInBackground()`.

---

## 5) الإعدادات الجديدة (جدول `settings`)

أُضيفت عبر `CourierSystemConfigService` وسيدر `CourierSystemConfigSeeder`:

| المفتاح | الافتراضي |
|---------|-----------|
| `cancel_unpaid_advance_hours` | `12` |
| `aggregate_sub_shipments_days` | `3` |
| `aggregate_sub_shipments_scope` | `all` (أو `warehouse` / `group`) |
| `aggregate_sub_shipments_target_id` | فارغ (رقم المستودع أو المحافظة حسب النطاق) |
| `execution_start_hours` | `24` |

---

## 6) صفحات الأدمن

### 6.1 صفحة الإعدادات «مؤقتات الشحن»
- المسار: `admin/settings/shipping-timers` (GET/PATCH) + `admin/settings/shipping-timers/reset` (POST).
- تعرض: ساعات العمل (بداية/نهاية)، قيم X وY وZ وN وW، ونطاق التجميع (مع اختيار المستودع أو المحافظة حسب النطاق).
- زر **استعادة القيم الافتراضية** مع نافذة تأكيد.
- بطاقة «آخر تحديث» تعرض من عدّل الإعدادات ومتى.
- تحقق من البيانات عبر `UpdateShippingTimersRequest` (التأكد من أن وقت النهاية بعد البداية، وأن اختيار النطاق يلزم اختيار الهدف).

### 6.2 صفحات مراقبة المهام
- القائمة: `admin/cron-jobs` — بطاقات لكل مهمة تعرض الحالة (نجاح/فشل/قيد التنفيذ/خاملة)، موعد آخر تشغيل والقادم، المدة، عدد المتأثرات، ومعدل النجاح (7 أيام / 30 يومًا)، وزر **تشغيل الآن**.
- التفاصيل: `admin/cron-jobs/{job}` — وصف المهمة، الإعدادات الحالية ذات الصلة، معدلات الصحة، وسجل التنفيذ مع فلاتر (الحالة / من تاريخ / إلى تاريخ) وتفاصيل كل تشغيل (JSON الخاص بالبيانات الإضافية).
- زر **تشغيل الآن**: `POST admin/cron-jobs/{job}/run-now`.

### 6.3 روابط القائمة الجانبية
- «مؤقتات الشحن» و«المهام المجدولة» أُضيفا للقائمة الجانبية في `layouts/admin.blade.php`.

### 6.4 الترجمات
- أُضيفت مفاتيح `cron_jobs` و `shipping_timers` في ملفي الترجمات (`lang/ar` و `lang/en`).

---

## 7) أذونات الموظفين

الصفحات الجديدة تتبع النمط المعتاد في اللوحة (تتحقق من صلاحيات الموظف قبل العرض):

- `admin.cron-jobs.index` / `admin.cron-jobs.show` / `admin.cron-jobs.run-now`
- `admin.settings.shipping-timers.index` / `admin.settings.shipping-timers.update`

> ملاحظة: هذه الأذونات تُستخدم في التحقق من الكود، ويُفضل إضافتها لجدول الأذونات عند الحاجة.

---

## 8) كيف تشغّلها وتراقبها

- **التشغيل اليدوي:** `php artisan cron:run courier-auto-reject` (أو أي مفتاح آخر).
- **المجدول:** أضِف إلى Crontab (أو Task Scheduler على Windows):
  `* * * * * cd /path/to/project && php artisan schedule:run >> /dev/null 2>&1`
- **المراقبة:** من لوحة الأدمن → «المهام المجدولة»، ثم افتح أي مهمة لرؤية سجلها وتشغيلها يدويًا.

---

## 9) التحقق المنفّذ

- تشغيل الهجرتين بنجاح.
- بذر الإعدادات الجديدة بنجاح.
- فحص بناء الجملة لجميع الملفات الجديدة (`php -l`).
- تشغيل المهام الخمس مرة واحدة يدويًا — كلها نجحت وسُجّلت في `cron_job_logs`.
- `php artisan schedule:list` يعرض المهام الخمس بالتوقيت الصحيح.
- فحص المسارات الجديدة (بتهيئة التطبيق مباشرة) — كلها مسجّلة وضمن وسيط `admin`.
- **ملاحظة فنية:** أمر `php artisan route:list` يفشل حاليًا بسبب خطأ قديم غير متعلق بنظامنا (`VendorUrgentTaskService`)؛ لذلك يُستخدم أسلوب التهيئة المباشرة للتحقق كما في التعديلات السابقة.
