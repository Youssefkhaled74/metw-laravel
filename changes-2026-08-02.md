# سجل تغييرات آخر تعديّين (Mol7a2 5 و file 5 changes)

> هذا الملف يشرح ببساطة كل ما تم تنفيذه في آخر تعديين على النظام:
>
> - **التعديل الأول:** `449dfa2` باسم **"Mol7a2 5"** (بتاريخ 2026-08-02).
> - **التعديل الثاني:** `da5e4c2` باسم **"file 5 changes"** (بتاريخ 2026-08-03).
>
> التعديلان معًا يطبّقان **الأقسام من 1 إلى 5 من "ملحق ملاحظات 5"**، أي بناء نظام **الشحن والتوصيل عبر المندوبين** من البداية حتى النهاية.

---

## 1) نظرة عامة على الفكرة

باختصار: عندما يطلب عميل (مستخدم أو تاجر) شحن طرد، يقوم النظام تلقائيًا بما يلي:

1. يحدّد **نوع الطلب** و**المسارات الممكنة** له (مسار مباشر، مسار عبر مخازن، مسار بالباص، توصيل سريع).
2. يعرض الطلب على **مندوبين مؤهلين** (حسب المحافظات والمدن والأوزان والفئة).
3. يقارن العروض، ويرشّح أفضل مندوب لكل جزء من المسار، ويحسب التكلفة.
4. يعرض المسارات الجاهزة على العميل **ليختار** واحدًا منها.
5. بعد الاختيار، يتم **دفع عربون (دفعة مقدمة)** ثم **بدء التنفيذ**.
6. إذا لم يقبل أي مندوب، يغلق الطلب تلقائيًا برسالة فشل **قابلة للتعديل من الأدمن**.

---

## 2) جداول قاعدة البيانات الجديدة

### 2.1 جدول `courier_assignments` (المندوبين المرشّحين للجزء)
- أُنشئ في **التعديل الأول** (ملف `2026_08_02_000002_create_courier_assignments_table.php`).
- يخزّن **عروض المندوبين** على أجزاء المسار: كل صف = عرض مندوب واحد على جزء واحد.
- يحتوي على: نوع الجزء (`leg_type`)، حالة العرض، رسوم العرض، وقت عرض الطلب، موعد آخر رد، إلخ.

### 2.2 جدول `request_paths` (المسارات المقترحة)
- أُنشئ في **التعديل الثاني** (ملف `2026_08_02_000010_create_request_paths_table.php`).
- كل مسار مقترح للطلب له صف واحد هنا.
- **Polymorphic**: المسار ممكن يكون تابعًا لطلب شحن (`ShipmentRequest`) أو لعنصر طلب (`OrderItem`) من تطبيق السوق.
- يخزّن: نوع المسار، حالة المسار، تفاصيل الأجزاء (JSON)، التكلفة الإجمالية والعملة، سبب الفشل، وأوقات المراحل (تأكيد المندوب، العرض على العميل، اختيار العميل، بدء التنفيذ، التنفيذ، الفشل).

### 2.3 جدول `advance_payments` (الدّفعات المقدّمة / العربون)
- أُنشئ في **التعديل الثاني** (ملف `2026_08_02_000011_create_advance_payments_table.php`).
- يخزّن **العربون** الذي يدفعه العميل مقابل المسار المختار.
- **Polymorphic** أيضًا: يتبع المسار نفسه، ويتم الدفع بواسطة مستخدم أو تاجر (`submitter_type`).
- يحتوي على: المبلغ والعملة، حالة الدفعة، طريقة الدفع ورقم المرجع، من قام بالاعتماد ومتى، وقت الرفض، وقت الاسترداد.

---

## 3) الأعمدة الجديدة على الجداول الموجودة

### 3.1 جدول `shipment_requests` (طلبات الشحن)
- من **التعديل الأول** (ملف `2026_08_02_000003_add_dispatch_columns_to_shipment_requests_table.php`):
  - `dispatch_state` — حالة التوزيع على المندوبين (بانتظار التوزيع / تم التوزيع / لا يوجد مندوب ...).
  - `dispatch_evaluated_at` — وقت آخر فحص للتوزيع.
  - `dispatch_result` — نتيجة التوزيع (تُحفظ كنص).
- من **التعديل الثاني** (ملف `2026_08_02_000012_add_request_path_columns_to_shipment_requests_table.php`):
  - `request_type` — نوع طلب الشحن (مسار واحد / مساران / ثلاثة / توصيل سريع).
  - `is_fast_delivery` — هل هو توصيل سريع (نعم/لا).
  - `submitter_type` + `submitter_id` — من قدّم الطلب (مستخدم أو تاجر).
  - `selected_request_path_id` — المسار الذي اختاره العميل (رابط لجداول المسارات).
  - `failure_text` — نص الفشل الذي يُعرض للعميل عندما لا يتوفر مندوب.
  - `paths_evaluated_at` — وقت آخر تقييم للمسارات.
  - `execution_started_at` — وقت بدء التنفيذ.
  - `closed_at` — وقت إغلاق الطلب.

### 3.2 جدول `order_items` (عناصر الطلبات في تطبيق السوق)
- من **التعديل الأول** (ملف `2026_08_02_000004_add_dispatch_columns_to_order_items_table.php`):
  - نفس أعمدة التوزيع: `dispatch_state` و `dispatch_evaluated_at` و `dispatch_result`.
- من **التعديل الثاني** (في ملف `app/Models/OrderItem.php`):
  - `representative_id` — المندوب المختار للتنفيذ.
  - `accepted_fee` — الرسوم المقبولة.
  - `accepted_at` — وقت قبول المندوب.

### 3.3 جدول `shipment_request_packages` (طُرود طلب الشحن)
- من **التعديل الأول** (ملف `2026_08_02_000005_add_consignment_type_to_shipment_request_packages_table.php`):
  - `consignment_type` — نوع الشحنة (كيفية تصنيف الطرد).

### 3.4 جدول `courier_assignments` (العروض)
- من **التعديل الثاني** (ملف `2026_08_02_000013_add_request_path_id_to_courier_assignments_table.php`):
  - `request_path_id` — يربط كل عرض (على جزء) بالمسار الذي ينتمي إليه.

---

## 4) الأنواع والمصطلحات الجديدة (Enums)

### 4.1 من **التعديل الأول**:
- `CourierCategory` — فئات الشحنة (فئة 1 / 2 / 3) حسب قدرة وسيلة النقل.
- `CourierRole` — دور المندوب المستنتج من أنواع عمله (توصيل محلي، شحن بين محافظات، سائق باص...).
- `DispatchState` — حالة التوزيع على المندوبين.
- `RequestLegType` — نوع الجزء في المسار (شحن، توصيل، باص، إلخ).
- `ShippingType` — نوع الشحن (مباشر، عبر مخازن، متعدد...).
- تم توسيع `CourierAssignmentStatus` بإضافة حالات جديدة مثل قبول المندوب للعرض.

### 4.2 من **التعديل الثاني**:
- `CourierRequestType` — نوع طلب الشحن (مسار واحد / مساران / ثلاثة / توصيل سريع)، وكل نوع له عدد المسارات المتوقع.
- `RequestPathType` — نوع المسار (شحن مباشر، عبر مخازن، عبر باص، توصيل سريع)، وكل نوع يعرّف أجزاءه.
- `RequestPathStatus` — حالات المسار: بانتظار مندوبين ← تأكيد المندوب ← العرض على العميل ← اختيار العميل ← التنفيذ ← تنفيذ بلا رجعة + فشل / إلغاء.
- `AdvancePaymentStatus` — حالات العربون: معلّق، مدفوع، مؤكَّد، مرفوض، مسترد.
- `SubmitterType` — من قدّم الطلب: مستخدم أو تاجر.
- تم توسيع `ShipmentRequestStatus` بحالات جديدة: جارٍ إيجاد مندوبين، بانتظار اختيار المسار، بانتظار دفع العربون، قيد التنفيذ، فشل لعدم توفر مندوب.
- تمت إضافة حالة `CONFIRMED` لعروض المندوبين: تعني أن النظام وافق تلقائيًا على هذا المندوب لهذا الجزء.

---

## 5) نماذج قاعدة البيانات (Models)

- `CourierAssignment` — نموذج عروض المندوبين مع الروابط والوظائف المساعدة (من التعديل الأول) + ربط المسار (من الثاني).
- `ConsignmentType` — نموذج بسيط لنوع الشحنة (الأول).
- `Representative` — أُضيفت له **وظائف مهمة** (الأول): معرفة أنواع العمل، الأدوار الممكنة، الحد الأقصى للوزن والحجم القابل للنقل، الفئات المدعومة، المحافظات والمدن التي يخدمها، وهل هو مفعّل ومعتمد.
- `ShipmentRequest` — أعمدة التوزيع والمسارات (الأول + الثاني).
- `ShipmentRequestPackage` — ربط نوع الشحنة (الأول).
- `OrderItem` — أعمدة التوزيع (الأول) + المندوب والرسوم (الثاني) + دالة `requestPaths()` للحصول على مسارات هذا العنصر.
- `RequestPath` — نموذج المسار مع علاقاته بالعروض والمدفوعات والعنصر الأب (الثاني).
- `AdvancePayment` — نموذج العربون مع علاقته بالمسار والمسؤول الذي اعتمده (الثاني).

---

## 6) خدمات المنطق (Services)

### 6.1 من **التعديل الأول**:
- `CourierRoleService` — تحويل أنواع العمل إلى أدوار مندوب ممكنة.
- `CourierMatchService` — **المطابقة**: يختار المندوبين المناسبين لكل جزء حسب المحافظات والمدن والوزن والحجم والفئة ومدى توفرهم.
- `CourierRequestProfile` — "بطاقة تعريف" للطلب تُستخدم في المطابقة (نقطة البداية، الوجهة، الوزن، الحجم، وجود قرية، نوع الشحن، الفئة).
- `WorkingHoursService` — حساب **ساعات العمل**: هل نحن داخل ساعات العمل؟ كم ساعة عمل بين وقتين؟ إضافة ساعات عمل لموعد مستقبلي (تُستخدم لحساب مواعيد الرد).
- `CourierTimeoutProcessor` — معالج **انتهاء المهلة**: يفحص كل دقيقة العروض التي انتهت مدتها ولم يرد عليها المندوب ويقوم بالإجراء التلقائي.
- `CourierSystemConfigService` — قراءة إعدادات النظام (ساعات العمل، مهلة الرد، عدد العروض لكل جزء، سلوك انتهاء المهلة).
- `CourierAssignmentService` — **التوزيع**: إرسال الطلب للمندوبين المناسبين، قبول/رفض/إنهاء العروض، والتعامل مع عدم استجابة المندوبين.
- `CourierDispatchService` — يبني المسارات ويرسل عروض المندوبين لكل جزء (بدأ في الأول واستُكمل في الثاني).
- `DispatchResult` — حاوية نتائج التوزيع (بدأت في الأول وطُوِّرت في الثاني لتشمل المسارات والعروض).

### 6.2 من **التعديل الثاني**:
- `CourierRequestTypeDetector` — **محدّد نوع الطلب**: يحلل بطاقة التعريف ويقرر النوع:
  - شحن داخل نفس المحافظة ← توصيل سريع.
  - شحن مباشر فقط ← مسار واحد.
  - شحن مباشر ومتعدد مع فئة 3 ← مساران.
  - غير ذلك ← ثلاثة مسارات.
- `CourierRequestWorkflowService` — **قلب النظام** (آلة الحالات):
  - **تأكيد المسارات**: عندما يكتمل كل جزء من المسار بمندوب واحد على الأقل، يختار أفضل مندوب لكل جزء (الأقل رسومًا ثم الأسرع استجابة)، يؤكده، يلغي بقية القابلين، ويحسب التكلفة الإجمالية.
  - **إنهاء المسارات الميتة**: المسار الذي لا يوجد له أي عروض معلّقة أو مقبولة يصبح فاشلًا.
  - **عرض المسارات على العميل**: يعرض كل المسارات الناجحة ويحوّل الطلب إلى "بانتظار اختيار المسار". إذا لم ينجح أي مسار، يغلق الطلب كفاشل مع نص الفشل المُهيأ.
  - **اختيار المسار**: يثبّت المسار المختار، يلغي بقية المسارات والعروض المعلقة. إذا كان الطلب من تاجر يبدأ التنفيذ فورًا؛ إذا كان من مستخدم ينتقل لدفع العربون.
  - **بدء التنفيذ**: يحوّل المسار والطلب إلى "قيد التنفيذ" ويعيّن مندوب أول جزء مؤكد كمسؤول التنفيذ.
  - **إنهاء عنصر طلب السوق**: عناصر الطلبات التي لا تملك مُقدِّم طلب: المسار المؤكد يبدأ التنفيذ مباشرة مع المندوب المؤكد.
- `AdvancePaymentService` — **إدارة العربون**:
  - إنشاء الدفعة المقدمة من المستخدم أو من التاجر (بمبلغ اختياري أو افتراضيًا = تكلفة المسار).
  - اعتماد الدفعة من الأدمن: تبدأ التنفيذ تلقائيًا لطلبات المستخدمين.
  - رفض الدفعة.
  - عند الإنشاء يُطلق حدث `AdvancePaymentSubmitted`.

---

## 7) واجهة التطبيق (API للمستخدم / تطبيق الشحن)

تمت إضافة هذه المسارات في `routes/api.php` تحت `api/v1/shipment/requests/{id}` (التعديل الثاني):

| الطريقة | المسار | الوظيفة |
|---------|--------|---------|
| GET | `/paths` | عرض مسارات الطلب مع عروض المندوبين لكل جزء. |
| GET | `/tracking` | حالة الطلب + المسارات + العربونات + نص الفشل (تتبّع). |
| POST | `/select-path` | اختيار مسار من المعروضة (مع تحقق من صحة البيانات). |
| POST | `/pay-advance` | تقديم دفعة العربون للمسار المختار. |

أيضًا:
- `ShipmentRequestPathController` — المتحكم المسؤول عن هذه الوظائف.
- أدوات تحقق جديدة: `SelectRequestPathRequest` و `PayAdvanceRequest`.
- موارد (Resources) جديدة لعرض البيانات بشكل منظم: `RequestPathResource` و `CourierAssignmentResource` و `AdvancePaymentResource`.
- تطوير `ShipmentRequestResource` ليشمل: نوع الطلب، هل هو توصيل سريع، المسار المختار، نص الفشل، وأوقات المراحل.

---

## 8) لوحة الأدمن (Admin Dashboard)

من **التعديل الثاني**:

- **صفحة العربونات**: `admin/advance-payments` — عرض الدفعات (مدفوعة/مؤكدة/مرفوضة) مع فلاتر وبحث، وأزرار **اعتماد** و **رفض**.
- **صفحة إعدادات رسائل الفشل**: `admin/settings/courier-failure-messages` — تعديل نص رسالة الفشل عندما لا يتوفر مندوب شحن، ونص الفشل عندما لا يتوفر مندوب توصيل.
- **صفحة تفاصيل طلب الشحن**: أُضيفت بطاقة "مسارات التوصيل والدفعات المقدمة" تعرض المسارات وحالاتها وأجزاءها وتكاليفها والمندوبين المؤكدين والمدفوعات.
- **روابط جانبية** جديدة في القائمة.
- **ترجمات** جديدة بالعربية والإنجليزية لكل ما سبق.

من **التعديل الأول**:
- تعديلات على القائمة الجانبية للوحة الأدمن (لإضافة الروابط الخاصة بنظام المندوبين).

---

## 9) الإعدادات والبيانات الأولية (Seeders)

- `CourierSystemConfigSeeder` — يزرع الإعدادات الافتراضية (تم توسيعه في التعديلين):
  - ساعات العمل: من 10:00 صباحًا إلى 10:00 مساءً.
  - ساعات الرد المسموحة: 3 ساعات عمل.
  - ساعات الرفض التلقائي: 7 ساعات عمل.
  - الإجراء عند انتهاء المهلة: إعادة العرض على المندوب التالي تلقائيًا.
  - الحد الأقصى لعروض المندوبين لكل جزء: 5.
  - نصا رسالة الفشل (شحن / توصيل) قابلان للتعديل من الأدمن.
- `DatabaseSeeder` — إضافة سيدر إعدادات نظام المندوبين إلى قائمة البذور الرئيسية.

---

## 10) المهام المجدولة (Scheduling)

- من **التعديل الثاني** (في `app/Console/Kernel.php`): مهمة مجدولة تعمل **كل دقيقة** لتنفيذ `CourierTimeoutProcessor` وتنتهي تلقائيًا أي عروض مندوبين تجاوزت وقت الرد.
- **ملاحظة فنية:** مزود `Schedule` غير موجود في أسماء Facade الافتراضية بالمشروع، لذلك سُجّلت المهمة داخل `Console\Kernel` بدلًا من `routes/console.php`.

---

## 11) ملاحظات وأمور يجب معرفتها

- العربونات المقدمة من **التاجر** تُسجّل في النظام، لكن التنفيذ يبدأ مباشرة عند اختيار المسار **بدون خطوة اعتماد من الأدمن** (حسب القسم 3 من الملحق).
- الرفض أو الاعتماد في لوحة الأدمن يعمل مع حساب الأدمن وحساب الموظف معًا.
- أمر `php artisan route:list` يفشل حاليًا بسبب خطأ قديم غير متعلق بنظامنا (`VendorUrgentTaskService` يحتاج معاملًا لا يمكن حقنه)، لذلك تم التحقق من المسارات الجديدة بطريقة أخرى (تهيئة التطبيق مباشرة). المسارات الجديدة كلها تعمل.
- كل الهجرات الجديدة (`2026_08_02_000001` حتى `2026_08_02_000013`) تم تنفيذها بنجاح (`Ran`).
